<UserControl x:Class="Client.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client"
             mc:Ignorable="d" 
             d:DesignHeight="370" d:DesignWidth="610">
    <Grid Background="#1D3557">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
            <Label Content="First Name:" Foreground="White" FontSize="17" Margin="10,12,5,0"/>
            <TextBox x:Name="FirstNameTextBox" Style="{StaticResource txt_box}" Text="{Binding Ime}" TextChanged="UserTextBox_TextChanged" />
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
            <Label Content="Last Name:" Foreground="White" FontSize="17" Margin="10,12,5,0"/>
            <TextBox x:Name="LastNameTextBox" Style="{StaticResource txt_box}" Text="{Binding Prezime}" TextChanged="UserTextBox_TextChanged" />
        </StackPanel>
        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal">
            <Label Content="Role:" Foreground="White" FontSize="17" Margin="10,12,50,0"/>
            <TextBox x:Name="RoleTextBox" Style="{StaticResource txt_box}" Text="{Binding Role}" IsEnabled="False" />
        </StackPanel>
        <StackPanel Grid.Row="3" Grid.Column="0" Orientation="Horizontal">
            <Label Content="Username:" Foreground="White" FontSize="17" Margin="10,12,8,0"/>
            <TextBox x:Name="UsernameTextBox" Style="{StaticResource txt_box}" Text="{Binding KorisnickoIme}" TextChanged="UserTextBox_TextChanged"/>
        </StackPanel>
        <StackPanel Grid.Row="4" Grid.Column="0" Orientation="Horizontal">
            <Label Content="Password:" Foreground="White" FontSize="17" Margin="10,12,13,0"/>
            <TextBox x:Name="PasswordTextBox" Style="{StaticResource txt_box}" Text="{Binding Lozinka}" TextChanged="UserTextBox_TextChanged" />
        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="4" Margin="0,10,0,0">
            <Image x:Name="ProfileImage" Source="/Images/profile.jpg" Width="200" Height="151"/>
            <Button x:Name="button_picture" Style="{StaticResource device_button}" Content="Change picture" Margin="0,20,0,0"  Click="button_picture_Click"/>
        </StackPanel>
        <StackPanel Grid.Row="5" Grid.Column="0" >
            <Button x:Name="button_save" Style="{StaticResource device_button}" Content="Save changes" Margin="0,20,0,0" IsEnabled="False" Click="button_save_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
