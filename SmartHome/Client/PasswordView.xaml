<UserControl x:Class="Client.PasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client"
             mc:Ignorable="d" 
              d:DesignHeight="370" d:DesignWidth="610">
    <Grid Background="#1D3557">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="63*"></RowDefinition>
            <RowDefinition Height="63*"></RowDefinition>
            <RowDefinition Height="231*"></RowDefinition>

        </Grid.RowDefinitions>


        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
            <Label Content="Current password:" Foreground="White" FontSize="17" Margin="10,12,15,0" Height="45"/>
            <TextBox x:Name="CurrentPasswordTextBox" Style="{StaticResource txt_box}"  TextChanged="CurrentPasswordTextBox_TextChanged" />
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
            <Label Content="New password:" Foreground="White" FontSize="17" Margin="10,12,37,0" Height="45"/>
            <TextBox x:Name="NewPasswordTextBox" Style="{StaticResource txt_box}" TextChanged="CurrentPasswordTextBox_TextChanged" />
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Top">
            <Button x:Name="button_save" Style="{StaticResource device_button}" Content="Save changes" Margin="170,10,0,0" IsEnabled="False" Click="button_save_Click" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
