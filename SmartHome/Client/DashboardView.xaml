<UserControl x:Class="Client.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client"
             xmlns:helpers="clr-namespace:Client.Helpers"
             mc:Ignorable="d" >
    <UserControl.DataContext>
        <helpers:TimeViewModel/>
    </UserControl.DataContext>
    <Grid Background="#1D3557" Height="380">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="110"></RowDefinition>
            <RowDefinition Height="110"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Rectangle Width="180" Height="100" Fill="#FF4773B2" Grid.Column="0" Grid.Row="0" RadiusX="20" RadiusY="20" Margin="6"/>
        <StackPanel Grid.Row="0" Grid.Column="0">
            <TextBlock x:Name="TimeTextBlock" Text="{Binding CurrentTime}" FontSize="25" Foreground="White" FontWeight="SemiBold" Margin="20,10,0,0"/>
            <TextBlock  x:Name="DateTextBlock" Text="{Binding CurrentDate}" FontSize="15" Foreground="White"  Margin="20,5,0,0"/>
            <TextBlock Text="Novi Sad,Serbia" FontSize="15" Foreground="White"  Margin="20,5,0,0"/>
        </StackPanel>
        <Border Grid.Row="1" Grid.Column="0" Width="180"
        Background="#4773B2"
        CornerRadius="20"
        Margin="6"
        Padding="10">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="53*"/>
                    <ColumnDefinition Width="0"/>
                    <ColumnDefinition Width="27*"/>
                </Grid.ColumnDefinitions>

                <!-- NETWORK -->
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Height="78" Width="106">
                    <TextBlock Text="🌐 Network"
                       FontSize="14"
                       Foreground="White"
                       HorizontalAlignment="Left"/>

                    <TextBlock x:Name="NetworkTextBlock"
                       Text="Disconnected"
                       FontSize="16"
                       Margin="0,15,0,0"
                       FontWeight="SemiBold"
                       Foreground="White"
                       HorizontalAlignment="Left" VerticalAlignment="Center" Width="102"/>
                </StackPanel>

                <!-- DIVIDER -->
                <Border Grid.Column="2"
                Margin="2,0,52,0"
                Background="#A8DADC"/>

                <!-- PORT -->
                <StackPanel Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" Height="78" Width="54">
                    <TextBlock Text="🔌 Port"
                       FontSize="14"
                       Foreground="White"
                       HorizontalAlignment="Center"/>

                    <TextBlock x:Name="PortTextBlock"
                       Text="43574"
                       FontSize="16"
                       Margin="10,15,0,0"
                       FontWeight="SemiBold"
                       Foreground="White"
                       HorizontalAlignment="Center"/>
                </StackPanel>

            </Grid>
        </Border>

        <Grid Grid.Row="2" Grid.ColumnSpan="2" Margin="0,8,11,0">

            <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="40"/>
                    </Grid.ColumnDefinitions>

                    <!-- LEFT -->
                <Button Grid.Column="0"
                Content="◀"
                Click="Left_Click"
                Background="Transparent"
                BorderThickness="0"
                Foreground="White"
                FontSize="15" VerticalAlignment="Center"
                Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- CAROUSEL -->
                    <ScrollViewer Grid.Column="1"
                      x:Name="DeviceScrollViewer"
                      HorizontalScrollBarVisibility="Hidden"
                      VerticalScrollBarVisibility="Disabled"
                      CanContentScroll="False">

                        <ItemsControl x:Name="DeviceItems">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                <Border Background="#4773B2"
                            CornerRadius="10"
                            Margin="6"
                            Padding="10"
                            Width="150"
                            Height="90">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                            <Setter Property="RenderTransform">
                                                <Setter.Value>
                                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                                </Setter.Value>
                                            </Setter>

                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="RenderTransform">
                                                        <Setter.Value>
                                                            <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>


                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Icon}" FontSize="18" Foreground="White"/>
                                        <TextBlock Text="{Binding Name}"
                                       Foreground="White"
                                       FontSize="14"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Status}"
                                       Foreground="#A8DADC"
                                       FontSize="17"
                                       FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Room}"
                                       Foreground="White"
                                       FontSize="12"/>
                                    </StackPanel>

                                </Border>
                            </DataTemplate>
                            </ItemsControl.ItemTemplate>

                        </ItemsControl>

                    </ScrollViewer>

                    <!-- RIGHT -->
                <Button Grid.Column="2"
                Content="▶"
                Click="Right_Click"
                Background="Transparent"
                BorderThickness="0"
                Foreground="White" VerticalAlignment="Center" Margin="20,0,0,0"
                FontSize="15"
                Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                </Grid>

            <Grid x:Name="LoadingOverlay"
      Background="#801D3557"
      Visibility="Visible">

                <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center">

                    <!-- SPINNER -->
                    <Ellipse Width="40"
                 Height="40"
                 Stroke="White"
                 StrokeThickness="4"
                 StrokeDashArray="2 2">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate"
                                 CenterX="20"
                                 CenterY="20"/>
                        </Ellipse.RenderTransform>
                    </Ellipse>

                    <TextBlock Text="Loading devices..."
                   Foreground="White"
                   Margin="10"
                   HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>


        </Grid>

        <StackPanel Grid.Column="1" Grid.RowSpan="2" Grid.Row="0" >
            <Image x:Name="HomeImage" Source="/Images/living_room.jpg"  Stretch="UniformToFill" Height="228" Width="390" Margin="0,5,10,0">
                <Image.Clip>
                    <RectangleGeometry Rect="0,0,390,215" RadiusX="30" RadiusY="20"/>
                </Image.Clip>
            </Image>

        </StackPanel>

    </Grid>
</UserControl>
