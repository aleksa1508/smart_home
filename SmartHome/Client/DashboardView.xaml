<UserControl x:Class="Client.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client"
             xmlns:helpers="clr-namespace:Client.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="370" d:DesignWidth="610">
    <UserControl.DataContext>
        <helpers:TimeViewModel/>
    </UserControl.DataContext>
    <Grid Background="#1D3557" Margin="0,20,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="110"></RowDefinition>
            <RowDefinition Height="110"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Rectangle Width="180" Height="100" Fill="#FF4773B2" Grid.Column="0" Grid.Row="0" RadiusX="20" RadiusY="20" Margin="6"/>
        <StackPanel Grid.Row="0" Grid.Column="0">
            <TextBlock x:Name="TimeTextBlock" Text="{Binding CurrentTime}" FontSize="25" Foreground="White" FontWeight="SemiBold" Margin="20,10,0,0"/>
            <TextBlock  x:Name="DateTextBlock" Text="{Binding CurrentDate}" FontSize="15" Foreground="White"  Margin="20,5,0,0"/>
            <TextBlock Text="Novi Sad,Serbia" FontSize="15" Foreground="White"  Margin="20,5,0,0"/>
        </StackPanel>
        <Rectangle Width="180" Height="100" Fill="#FF4773B2" Grid.Column="0" Grid.Row="1" RadiusX="20" RadiusY="20" Margin="6"/>
        <StackPanel Grid.Row="1" Grid.Column="0">
            <Grid Margin="20,20,0,0" Height="80">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="2*"></RowDefinition>
                </Grid.RowDefinitions>
                <TextBlock Text="Network" FontSize="18" Foreground="White" FontWeight="Medium" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center"/>
                <TextBlock Text="Port" FontSize="18" Foreground="White" FontWeight="Medium" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center"/>
                <TextBlock x:Name="NetworkTextBlock" Text="Connected" FontSize="18" Foreground="White" FontWeight="Medium" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBlock x:Name="PortTextBlock" Text="43574" FontSize="18" Foreground="White" FontWeight="Medium" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center"  VerticalAlignment="Center"/>
                <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Margin="16,0,85,0"
                BorderBrush="White" BorderThickness="0,0,1,0" Grid.ColumnSpan="2"/>
            </Grid>

        </StackPanel>
        <Rectangle Width="580" Fill="#FF4773B2" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" RadiusX="20" RadiusY="20" Margin="15,8,15,10"/>
        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" >

        </StackPanel>
        <StackPanel Grid.Column="1" Grid.RowSpan="2" Grid.Row="0" >
            <Image x:Name="HomeImage" Source="/Images/living_room.jpg"  Stretch="UniformToFill" Height="228" Width="390" Margin="0,5,10,0">
                <Image.Clip>
                    <RectangleGeometry Rect="0,0,390,215" RadiusX="30" RadiusY="20"/>
                </Image.Clip>
            </Image>

        </StackPanel>

    </Grid>
</UserControl>
